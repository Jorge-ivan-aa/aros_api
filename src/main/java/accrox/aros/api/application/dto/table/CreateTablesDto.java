package accrox.aros.api.application.dto.table;

/**
 * DTO used by application layer to request creation of multiple tables.
 * Converted to a record following the project's DTO style.
 */
public record CreateTablesDto(
        int count) {
}
